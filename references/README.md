# References

Project-wide source material used across episodes.

- `berton-2023-pdftotext.txt` — PDF text extraction using `pdftotext -layout`, useful for equations and tables.
- `berton-2023-raw.txt` — PDF text extraction using `pdftotext -raw`, useful as a prose fallback.
